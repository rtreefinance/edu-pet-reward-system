# 小宠物学习激励系统 — 使用操作手册

---

## 目录

1. [系统简介](#1-系统简介)
2. [快速开始（部署）](#2-快速开始部署)
3. [通用操作（三种角色相同）](#3-通用操作三种角色相同)
4. [学生端操作指南](#4-学生端操作指南)
5. [教师端操作指南](#5-教师端操作指南)
6. [家长端操作指南](#6-家长端操作指南)
7. [金币与宠物规则](#7-金币与宠物规则)
8. [常见问题](#8-常见问题)

---

## 1. 系统简介

本系统是一个面向小学生的学习激励 Web 应用，通过"做作业 → 赚金币 → 养宠物"的闭环，激发孩子的学习动力。

三大核心模块：

| 模块 | 说明 |
|------|------|
| 作业评分 | 教师布置作业 → 批改打分 → 满分自动奖励 50 金币 |
| 每日签到 | 学生每天打卡 → 基础 +10 金币 → 连续 7 天额外 +50 金币 |
| 虚拟宠物 | 领养宠物 → 喂食/装扮/升级 → 等级 3/6/10/50 触发进化 |

三种角色：

- 学生：交作业、签到、养宠物（核心交互）
- 教师：布置作业、批改打分（管理）
- 家长：查看孩子学习数据（只读）

---

## 2. 快速开始（部署）

### 前置条件

- Node.js 18+
- Docker 与 Docker Compose
- MongoDB（docker-compose 已内置）

### 使用 Docker 一键启动

```bash
# 克隆项目
git clone https://github.com/rtreefinance/edu-pet-reward-system.git
cd edu-pet-reward-system

# 启动所有服务（MongoDB + 后端 + 前端）
docker-compose up -d

# 初始化种子数据（创建默认教师和测试学生）
docker-compose exec server npm run seed
```

启动后：

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:3000 |
| 后端 API | http://localhost:4000 |
| MongoDB | localhost:27017 |

### 本地开发模式

```bash
# 安装依赖
cd src/server && npm install
cd ../client && npm install

# 启动后端
cd src/server && npm run dev    # → http://localhost:4000

# 启动前端（另一个终端）
cd src/client && npm run dev    # → http://localhost:5173
```

### 种子数据默认账号

执行 `npm run seed` 后，系统会创建以下测试账号：

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 教师 | teacher1 | password123 |
| 学生 | student1 | password123 |
| 学生 | student2 | password123 |
| 家长 | parent1 | password123 |

---

## 3. 通用操作（三种角色相同）

### 3.1 注册

打开首页 → 点击"注册" → 填写信息：

- 用户名：英文/数字，4–20 位
- 密码：至少 6 位
- 角色：选择 学生 / 教师 / 家长
- 显示名称：中文名或昵称
- 教师需额外填写：任教科目
- 家长注册时可选填：关联码（后续版本支持）

提交后自动登录，跳转到对应角色的首页。

### 3.2 登录

输入已注册的用户名和密码，点击登录。

### 3.3 个人信息

顶部导航栏可查看当前登录用户信息。教师和家长可查看角色标识，学生可查看金币余额。

### 3.4 退出登录

点击右上角用户头像 → 退出登录。

---

## 4. 学生端操作指南

学生端是本系统的核心，包含四个主要功能页面。

### 4.1 首页（Dashboard）

登录后首先进入首页，概览展示：

- 金币余额（大号显示，带金币动画）
- 今日签到状态（已签到 / 未签到 → 点击签到按钮）
- 连续签到天数（进度条）
- 宠物小卡片（名称、等级、饥饿值、心情值）
- 最近作业列表（待完成 / 已批改）

### 4.2 作业列表

底部导航 → 点击"作业"图标。

页面展示所有作业，按科目和状态分类：

- 待提交（pending）：灰色标签
- 已提交（submitted）：蓝色标签
- 已批改（graded）：绿色标签 + 分数

点击作业卡片查看详情（题目、截止日期、得分、教师评语）。

### 4.3 签到日历

底部导航 → 点击"签到"图标。

页面展示：
- 当月日历，已签到日期以金色标记
- 连续签到天数（"你已经连续签到 X 天！"）
- 签到按钮（每天只能点一次）

签到操作：
1. 点击"立即签到"按钮
2. 获得 +10 金币（弹窗提示）
3. 如果连续签到第 7 天，额外 +50 金币（弹窗特效）
4. 宠物心情值 +5

注意事项：
- 每天只能签到一次
- 如果断签（昨天没签到），连续天数重置为 1
- 签到时间为服务器时间（北京时间）

### 4.4 宠物

底部导航 → 点击"宠物"图标。

#### 4.4.1 领养宠物（首次使用）

如果还没有宠物，会看到领养页面：
1. 给宠物起名字（最多 30 字）
2. 选择宠物种类（如：小猫咪、小狗狗、小龙、小兔子）
3. 点击"领养"

宠物初始状态：等级 1、饥饿值 100、心情值 100。

#### 4.4.2 宠物主页

展示：
- 宠物大图（根据等级和皮肤显示）
- 名称和等级
- 饥饿值进度条（0–100，红色到绿色渐变）
- 心情值进度条（0–100，蓝色到粉色渐变）
- 已拥有装饰品列表
- 进化历史（时间线展示）

#### 4.4.3 喂食

点击"喂食"按钮：
- 消耗 5 金币
- 饥饿值 +20（上限 100）
- 心情值 +10（上限 100）
- 金币不足时会提示"金币不足，无法喂食"
- 宠物吃饱时（饥饿值 =100）会提示"宠物已经吃饱了"

#### 4.4.4 宠物商店

点击"商店"按钮进入购买页面。

可购买物品：

| 商品 | 图标 | 价格 | 类型 |
|------|------|------|------|
| 红色蝴蝶结 | 🎀 | 20 金币 | 装饰品 |
| 星星发卡 | ⭐ | 25 金币 | 装饰品 |
| 墨镜 | 🕶️ | 30 金币 | 装饰品 |
| 小皇冠 | 👑 | 50 金币 | 装饰品 |
| 围巾 | 🧣 | 15 金币 | 装饰品 |
| 火焰皮肤 | 🔥 | 100 金币 | 皮肤 |
| 冰霜皮肤 | ❄️ | 100 金币 | 皮肤 |
| 彩虹皮肤 | 🌈 | 150 金币 | 皮肤 |

购买操作：
1. 点击心仪的物品
2. 确认购买（扣金币）
3. 装饰品立即装备到宠物身上
4. 皮肤立即替换宠物外观
5. 不能重复购买已拥有的装饰品

#### 4.4.5 宠物进化和升级

宠物通过以下方式获得经验值（XP）：
- 完成作业（根据得分）
- 每日签到
- 喂食

等级进化路线（5 个阶段）：

| 等级 | 阶段 | 外观变化 |
|------|------|----------|
| 1–2 | 幼体 (base) | 初始形态 |
| 3–5 | 成长体 (stage2) | 变大一号 |
| 6–9 | 成熟体 (stage3) | 长出特征 |
| 10–49 | 完全体 (stage4) | 华丽外观 |
| 50+ | 究极体 (ultimate) | 终极形态 |

当等级达到 3、6、10、50 时，系统自动触发进化动画。进化记录保存在"进化时间线"中，可随时查看。

#### 4.4.6 饥饿值衰减机制

- 每 24 小时：饥饿值自动 -20
- 饥饿值降到 0 后不会继续降低
- 心情值随饥饿值降低而缓慢下降
- 长期不喂食会导致宠物心情低落，但不影响其他功能

---

## 5. 教师端操作指南

### 5.1 教师首页

登录教师账号后进入管理首页，展示：
- 所教科目和班级信息
- 本月布置作业总数
- 待批改作业数量
- 学生列表管理入口

### 5.2 布置作业

操作步骤：
1. 进入"布置作业"页面
2. 选择目标学生（下拉列表或搜索）
3. 填写作业信息：
   - 作业标题（必填，如"语文第一单元练习"）
   - 作业描述（选填，详细说明）
   - 科目（自动填充教师任教科目）
   - 截止日期（日期选择器）
   - 满分（默认 100，可自定义）
4. 点击"布置" → 作业发送到学生端

### 5.3 批改作业

操作步骤：
1. 进入"批改作业"页面
2. 查看待批改作业列表（按布置时间排序）
3. 点击作业卡片进入详情
4. 输入分数（0–100）
5. 点击"提交评分"

系统自动处理：
- 分数 = 满分 → 学生获得 +50 金币（作业满分奖励）
- 分数 < 满分 → 无金币奖励
- 作业状态变为"已批改"，不可再次评分
- 金币奖励记录写入交易流水

注意事项：
- 只能批改自己布置的作业
- 已批改的作业不能重复评分
- 评分后学生端即刻更新

### 5.4 学生管理

可查看所教学生的：
- 基本信息（姓名、等级）
- 作业完成情况
- 签到记录
- 当前金币余额

---

## 6. 家长端操作指南

家长端为只读模式，可以查看绑定孩子的学习数据。

### 6.1 家长首页

登录后展示：
- 绑定孩子列表
- 选择孩子后展示数据总览

### 6.2 查看孩子作业

操作步骤：
1. 在首页点击孩子姓名
2. 进入"作业"标签
3. 查看该孩子的：
   - 所有作业列表（按状态分类）
   - 每份作业的得分和教师评语
   - 作业提交时间

### 6.3 查看签到记录

选择孩子后 → "签到"标签 → 查看：
- 当月签到日历（已签到日期以金色标记）
- 连续签到天数
- 本月签到总天数

### 6.4 查看宠物

选择孩子后 → "宠物"标签 → 查看：
- 宠物名称、种类、等级
- 饥饿值和心情值
- 已购买装饰品
- 进化历史

家长只能查看，不能喂食、购买或任何修改操作。

---

## 7. 金币与宠物规则

### 7.1 金币规则速查表

| 行为 | 金币变化 | 条件 |
|------|----------|------|
| 作业满分 | +50 | 教师评分等于最大分数 |
| 每日签到 | +10 | 每天一次 |
| 连续7天签到 | +50 | 第 7/14/21/28…天触发 |
| 喂食宠物 | -5 | 每次喂食 |
| 购买装饰品 | -15 至 -50 | 视商品而定 |
| 购买皮肤 | -100 至 -150 | 视商品而定 |

### 7.2 宠物状态速查表

| 机制 | 变化 | 频率 |
|------|------|------|
| 饥饿衰减 | -20 | 每 24 小时 |
| 喂食恢复 | +20 饥饿 +10 心情 | 每次喂食 |
| 签到加成 | +5 心情 | 每次签到 |
| 进化检测 | Lv 3/6/10/50 | 升级时自动触发 |

### 7.3 进化阶段

| 等级范围 | 阶段名称 | 外观后缀 |
|----------|----------|----------|
| 1–2 | 幼体 | _base |
| 3–5 | 成长体 | _stage2 |
| 6–9 | 成熟体 | _stage3 |
| 10–49 | 完全体 | _stage4 |
| 50+ | 究极体 | _ultimate |

---

## 8. 常见问题

### Q1: 签到断了一天，之前的连续记录全没了？

连续签到天数以"昨天是否签到"为判断依据。如果昨天没签到，今天签到 streak 从 1 重新计算。但之前的签到记录不会消失，可在日历上看到。

### Q2: 宠物饥饿值降到 0 会怎么样？

宠物不会"死亡"。饥饿值为 0 时，停止衰减，宠物心情值会很低，但在页面上会显示饥饿动画提示你喂食。喂食后即可恢复。

### Q3: 教师能给学生加金币吗？

目前版本教师不能直接给学生加金币。金币只能通过作业满分、签到和连续签到获得。后续版本将支持教师奖励功能。

### Q4: 家长能解绑孩子吗？

目前版本家长与孩子的绑定通过注册时设置。后续版本将增加绑定管理功能。

### Q5: 忘记密码怎么办？

目前版本请找系统管理员重置密码。后续版本将增加密码重置功能。

### Q6: 多个设备能同时登录同一个账号吗？

可以。系统使用 JWT Token 认证，不同设备可以独立登录。但同一个账号的宠物状态和金币是共享的。

### Q7: 前端页面打不开？

确认 Docker 容器是否正常运行：

```bash
docker-compose ps
# 应看到 server、client、mongodb 三个服务都是 Up 状态
```

如果某个服务 DOWN，查看日志：

```bash
docker-compose logs server
docker-compose logs client
```

### Q8: 想添加更多宠物种类？

修改 `src/server/routes/pet.ts` 中的 `SPECIES` 数组即可。需要同时在前端添加对应的图片资源。

---

## 附录：API 路由速查

### 认证

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/auth/register | 注册 | 任何人 |
| POST | /api/auth/login | 登录 | 任何人 |
| GET | /api/auth/me | 获取当前用户 | 已登录 |

### 作业

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/homework | 布置作业 | 教师 |
| GET | /api/homework | 查看自己的作业 | 学生 |
| GET | /api/homework/child/:childId | 查看孩子的作业 | 家长 |
| GET | /api/homework/:id | 作业详情 | 学生/教师/家长 |
| PUT | /api/homework/:id/grade | 批改作业 | 教师 |

### 签到

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/checkin | 签到 | 学生 |
| GET | /api/checkin | 当月签到日历 | 学生 |
| GET | /api/checkin/streak | 连续签到天数 | 学生 |
| GET | /api/checkin/child/:childId | 孩子签到记录 | 家长 |

### 宠物

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/pet | 查看自己的宠物 | 学生 |
| POST | /api/pet | 领养宠物 | 学生 |
| POST | /api/pet/feed | 喂食 | 学生 |
| GET | /api/pet/shop | 商店列表 | 任何人 |
| POST | /api/pet/buy-accessory | 购买装饰品 | 学生 |
| GET | /api/pet/evolution | 进化历史 | 学生 |
| GET | /api/pet/child/:childId | 孩子宠物 | 家长 |

---

*文档版本: 1.0 | 最后更新: 2026-05-06*
